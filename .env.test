# Test-only environment contract — single source for the local/CI test DB URL.
# Not a real secret: matches docker/docker-compose.yml's dev Postgres defaults.
# Read by vitest.config.ts (local) and .github/workflows/ci.yml (CI).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/growth_db?schema=public
